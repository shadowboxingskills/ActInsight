import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.actinsight.app',
  appName: 'actinsight',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
